# Bootstrap metadata only. Capabilities, checks and actions are discovered
# at runtime from the plugin process (CAPABILITIES message) — never here.
id: system
name: System Diagnostics
version: 0.1.0
api_version: 1
description: Cross-platform system diagnostics (CPU, memory, disk, processes…)
platforms: [linux, windows]
architectures: [x86_64, aarch64]
executable:
  linux: robot-doctor-plugin-system
  windows: robot-doctor-plugin-system.exe
